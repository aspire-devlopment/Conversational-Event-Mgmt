/**
 * File: apiPaths.js
 * Purpose: API route path constants
 * Description: Centralized API endpoint paths for v1 REST API (/api/v1):
 *              Users, Roles, Events, Chat Sessions, Event Roles.
 */

const API_PATHS = {
  V1: '/api/v1',
  USERS: '/users',
  ROLES: '/roles',
  EVENTS: '/events',
  CHAT_SESSIONS: '/chat-sessions',
  EVENT_ROLES: '/event-roles',
};

module.exports = API_PATHS;

